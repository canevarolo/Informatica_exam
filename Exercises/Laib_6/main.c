#include <stdio.h>
#include <stdlib.h>

int main()
{
    int A[1000], B[1000], C[1000], i, k, j;

    for (i=0; i<1000; i++) {
            for (k=0; k<1000; k++){
                    for (j=0; j<1000; j++) {
                                if ((A[i])^2 == (B(k))^2 + (C(j))^2)
                                    printf ("La terna %d %d %d e' pitagorica", A[i], B[k], C[j]);
                                            }
                                   }
                            }
return 0;
}
